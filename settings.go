package main

// DB and http server settings
const (
	dateFormat = "20060102"
	DBDriver   = "sqlite3"
	DBFile     = "scheduler.db"
	port       = 7540
)

// SQL query const
const (
	limit50  int = 50
	limit100 int = 100
)

// Login settings
const (
	Token = ``
)
